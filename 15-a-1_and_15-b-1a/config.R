# configurations for 15.a.1/15.b.1
# save the .ods SID file as a .csv in the Input folder
# NB: ensure the AmountExtended column (the gross ODA) is formatted as numbers
# NB: ensure the decimal places are expanded to at least 4dp 
# (otherwise there may be rounding discrepancies when comparing sums between the R code output and the original ods source file)


filename <- "dataunderSID-Final2020.csv"

input_folder  <- "Example_Input"
